(function (angular) {

    angular.module("mainModule").service("taskService",
        ['$http', 'routePrefix', '$window',
            function ($http, routePrefix, $window) {

                return {

                  //methods will be added later
                }
            }
        ]);
})(angular);